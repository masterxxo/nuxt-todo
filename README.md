# Simple todo app

## Setup

Make sure to install the dependencies:

```bash
# npm
npm install
```

## Configuration

Before running the app, create a `.env` file in the root of the project making a copy of `.env.example`.

## Development Server

Start the development server on `http://localhost:3000`:

```bash
# npm
npm run dev
```
